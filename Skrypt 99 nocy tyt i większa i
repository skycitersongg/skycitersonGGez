-- [[ skycitersongg - Admin, Team, Config & Rekord Suite 6.0v ]] --
local Players, CoreGui, TweenService, UserInputService, RunService = game:GetService("Players"), game:GetService("CoreGui"), game:GetService("TweenService"), game:GetService("UserInputService"), game:GetService("RunService")
local LP = Players.LocalPlayer

-- =========================================================================
--  STAŁA BAZA DANYCH RANG (GLOBALNA / OFFLINE / AFK)
-- =========================================================================
_G.BialaLista = _G.BialaLista or {} 
_G.Wspolpracownicy = _G.Wspolpracownicy or { ["barltek8"] = true }
_G.TesterzyRangi = _G.TesterzyRangi or { ["Drugie9"] = true }
_G.ModeratorzyRangi = _G.ModeratorzyRangi or { ["Drugie9"] = true }
_G.CzarnaLista = _G.CzarnaLista or {}

local AktywaneToggles = {}
local ZapisaneFunkcje = {}

local function PobierzTagRangi(name)
    if name == "Demon0x0x1" then return "[ahp]", Color3.fromRGB(255, 215, 0)
    elseif _G.Wspolpracownicy[name] then return "[wsp]", Color3.fromRGB(0, 180, 255)
    elseif _G.ModeratorzyRangi[name] then return "[mhp]", Color3.fromRGB(255, 50, 50)
    elseif _G.TesterzyRangi[name] then return "[wdp]", Color3.fromRGB(255, 165, 0)
    elseif _G.BialaLista[name] then return "[ksgg.kom]", Color3.fromRGB(180, 50, 255)
    else return "", Color3.fromRGB(255,255,255) end
end

local function MaDostepDoKadry(name)
    return name == "Demon0x0x1" or _G.Wspolpracownicy[name] or _G.TesterzyRangi[name] or _G.ModeratorzyRangi[name]
end

local function MaDostepDoPremium(name)
    return name == "Demon0x0x1" or _G.BialaLista[name] or MaDostepDoKadry(name)
end

-- Zabezpieczenie przed Czarną Listą
if _G.CzarnaLista[LP.Name] then
    pcall(function()
        local ErrorGui = Instance.new("ScreenGui", CoreGui) local Lbl = Instance.new("TextLabel", ErrorGui)
        Lbl.Size = UDim2.new(1, 0, 1, 0) Lbl.BackgroundColor3 = Color3.fromRGB(30, 0, 0)
        Lbl.Text = "ZOSTALES ZABLOKOWANY PRZEZ ADMINISTRATORA! Zglos sie na Discord."
        Lbl.TextColor3 = Color3.fromRGB(255, 50, 50) Lbl.Font = Enum.Font.GothamBold Lbl.TextSize = 22
    end)
    return
end

-- Blokada zakładki ggFVIPWLbey dla darmowych graczy
local function SprawdzDostepDoTabu()
    if MaDostepDoPremium(LP.Name) then return true else
        pcall(function() 
            game:GetService("StarterGui"):SetCore("ChatMakeSystemMessage", {
                Text = "[skycitersongg]: BLOKADA! Zakladka ggFVIPWLbey kosztuje 16.99 ZL. Wejdz na nasz Discord, aby ja odblokowac: discord.gg/ugfJBg54n",
                Color = Color3.fromRGB(255, 50, 50), Font = Enum.Font.GothamBold
            })
        end)
        return false
    end
end

-- Tagi nad głową
local function StworzTagNadGlowa(player)
    if player.Character and player.Character:FindFirstChild("Head") then
        local head = player.Character.Head
        if head:FindFirstChild("AdminTagGui") then head.AdminTagGui:Destroy() end
        local tagText, tagColor = PobierzTagRangi(player.Name)
        if tagText == "" then return end
        local bb = Instance.new("BillboardGui", head)
        bb.Name = "AdminTagGui" bb.Size = UDim2.new(0, 200, 0, 50) bb.AlwaysOnTop = true bb.StudsOffset = Vector3.new(0, 2.5, 0)
        local lbl = Instance.new("TextLabel", bb)
        lbl.Size = UDim2.new(1, 0, 1, 0) lbl.BackgroundTransparency = 1 lbl.Text = tagText lbl.TextColor3 = tagColor lbl.Font = Enum.Font.GothamBold lbl.TextSize = 14 lbl.TextStrokeTransparency = 0.5
    end
end

if MaDostepDoKadry(LP.Name) then
    for _, p in pairs(Players:GetPlayers()) do pcall(function() StworzTagNadGlowa(p) p.CharacterAdded:Connect(function() task.wait(1) StworzTagNadGlowa(p) end) end) end
    Players.PlayerAdded:Connect(function(p) p.CharacterAdded:Connect(function() task.wait(1) StworzTagNadGlowa(p) end) end)
end

if CoreGui:FindFirstChild("skycitersgg") then CoreGui.skycitersgg:Destroy() end
if CoreGui:FindFirstChild("SkyArrowsGui") then CoreGui.SkyArrowsGui:Destroy() end
if CoreGui:FindFirstChild("YksArrowsGui") then CoreGui.YksArrowsGui:Destroy() end
if CoreGui:FindFirstChild("SkyIntroGui") then CoreGui.SkyIntroGui:Destroy() end
local TC = Color3.fromRGB(0, 230, 115)

-- INTRO POWITALNE
local IG = Instance.new("ScreenGui", CoreGui) IG.Name = "SkyIntroGui"
local IF = Instance.new("Frame", IG) IF.Size = UDim2.new(1, 0, 1, 0) IF.BackgroundColor3 = Color3.fromRGB(5, 10, 5) IF.BackgroundTransparency = 0.2
local GT = Instance.new("TextLabel", IF) GT.Size = UDim2.new(1, 0, 1, 0) GT.BackgroundTransparency = 1 GT.Text = "GG" GT.TextColor3 = TC GT.TextSize = 80 GT.Font = Enum.Font.Code
local ga = true task.spawn(function() while ga do GT.Text = ({ "G#", "G⚡", "☣G", "GG", "G_G" })[math.random(1, 5)] GT.TextColor3 = math.random(1, 2) == 1 and TC or Color3.fromRGB(255, 50, 50) task.wait(0.05) end end)
task.wait(2) ga = false GT.Text = "studio ggcitersony" GT.TextColor3 = Color3.fromRGB(255, 215, 0) GT.TextSize = 45 GT.Font = Enum.Font.GothamBold task.wait(1.5)
TweenService:Create(IF, TweenInfo.new(0.5), { BackgroundTransparency = 1 }):Play() TweenService:Create(GT, TweenInfo.new(0.5), { TextTransparency = 1 }):Play() task.delay(0.5, function() IG:Destroy() end)

-- PANEL INTERFEJSU (Z MOBILNYM PRZESUWANIEM I MINIMALIZACJĄ DO KÓŁKA)
local SG = Instance.new("ScreenGui", CoreGui) SG.Name = "skycitersgg" SG.ResetOnSpawn = false
local MF = Instance.new("Frame", SG) MF.Name = "MainFrame" MF.Size = UDim2.new(0, 550, 0, 380) MF.Position = UDim2.new(0.5, -275, 0.5, -190) MF.BackgroundColor3 = Color3.fromRGB(15, 22, 15)
local US = Instance.new("UIStroke", MF) US.Color = TC US.Thickness = 2 Instance.new("UICorner", MF).CornerRadius = UDim.new(0, 8)
local H = Instance.new("Frame", MF) H.Name = "Header" H.Size = UDim2.new(1, 0, 0, 45) H.BackgroundColor3 = Color3.fromRGB(10, 15, 10) Instance.new("UICorner", H).CornerRadius = UDim.new(0, 8)

-- KÓŁKO ZMINIMALIZOWANE "GG"
local MinBtn = Instance.new("TextButton", H) MinBtn.Size = UDim2.new(0, 30, 0, 30) MinBtn.Position = UDim2.new(1, -35, 0.5, -15) MinBtn.BackgroundColor3 = Color3.fromRGB(25, 35, 25) MinBtn.Text = "-" MinBtn.TextColor3 = TC MinBtn.Font = Enum.Font.GothamBold MinBtn.TextSize = 18 Instance.new("UICorner", MinBtn)
local CircleGui = Instance.new("Frame", SG) CircleGui.Name = "CircleGG" CircleGui.Size = UDim2.new(0, 60, 0, 60) CircleGui.Position = UDim2.new(0.1, 0, 0.5, -30) CircleGui.BackgroundColor3 = Color3.fromRGB(15, 22, 15) CircleGui.Visible = false Instance.new("UICorner", CircleGui).CornerRadius = UDim.new(0, 30) local CStroke = Instance.new("UIStroke", CircleGui) CStroke.Color = TC CStroke.Thickness = 2
local CircleOpen = Instance.new("TextButton", CircleGui) CircleOpen.Size = UDim2.new(1, 0, 1, 0) CircleOpen.BackgroundTransparency = 1 CircleOpen.Text = "GG" CircleOpen.TextColor3 = Color3.fromRGB(255, 215, 0) CircleOpen.Font = Enum.Font.GothamBold CircleOpen.TextSize = 20

MinBtn.MouseButton1Click:Connect(function() MF.Visible = false CircleGui.Visible = true end)
CircleOpen.MouseButton1Click:Connect(function() MF.Visible = true CircleGui.Visible = false end)

-- Przesuwanie kółka i panelu palcem/myszką
local function MakeDraggable(obj, target)
    local drag, dIn, dStart, sPos = false, nil, nil, nil
    obj.InputBegan:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then drag = true dStart = i.Position sPos = target.Position i.Changed:Connect(function() if i.UserInputState == Enum.UserInputState.End then drag = false end end) end end)
    obj.InputChanged:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch then dIn = i end end)
    UserInputService.InputChanged:Connect(function(i) if i == dIn and drag then local d = i.Position - dStart target.Position = UDim2.new(sPos.X.Scale, sPos.X.Offset + d.X, sPos.Y.Scale, sPos.Y.Offset + d.Y) end end)
end
MakeDraggable(H, MF) MakeDraggable(CircleGui, CircleGui)

local TagLabel = Instance.new("TextLabel", H) TagLabel.Size = UDim2.new(0, 100, 0, 20) TagLabel.Position = UDim2.new(0.5, -50, 0, 2) TagLabel.BackgroundTransparency = 1 TagLabel.Text = PobierzTagRangi(LP.Name) TagLabel.TextColor3 = select(2, PobierzTagRangi(LP.Name)) TagLabel.Font = Enum.Font.GothamBold TagLabel.TextSize = 12 TagLabel.TextAlignment = Enum.TextAlignment.Center
local TGG = Instance.new("TextLabel", H) TGG.Size = UDim2.new(1, 0, 1, 0) TGG.BackgroundTransparency = 1 TGG.Text = "GG" TGG.TextColor3 = Color3.fromRGB(255, 215, 0) TGG.TextSize = 24 TGG.Font = Enum.Font.GothamBold TGG.TextAlignment = Enum.TextAlignment.Center
local ST = Instance.new("TextLabel", H) ST.Size = UDim2.new(0, 120, 0, 20) ST.Position = UDim2.new(0.5, 25, 0.5, -2) ST.BackgroundTransparency = 1 ST.Text = "skyciters" ST.TextColor3 = TC ST.TextSize = 14 ST.Font = Enum.Font.GothamSemibold
local E2C = { US, ST, CStroke }
local SB = Instance.new("Frame", MF) SB.Name = "Sidebar" SB.Size = UDim2.new(0, 140, 1, -45) SB.Position = UDim2.new(0, 0, 0, 45) SB.BackgroundColor3 = Color3.fromRGB(10, 12, 10) Instance.new("UIListLayout", SB).Padding = UDim.new(0, 4)
local CA = Instance.new("Frame", MF) CA.Name = "ContentArea" CA.Size = UDim2.new(1, -150, 1, -55) CA.Position = UDim2.new(0, 145, 0, 50) CA.BackgroundTransparency = 1

local tabs = { "Main", "Auto", "Day Farm", "Bring Stuff", "Misc", "Local Player", "Theme", "Rekord", "ggFVIPWLbey" }
if MaDostepDoKadry(LP.Name) then table.insert(tabs, "Hacker Chat") end
if LP.Name == "Demon0x0x1" then table.insert(tabs, "Administracja Demon0x0x1") end
table.insert(tabs, "script player v.i.p")
local ab = {}

for i, t in ipairs(tabs) do
local pn = t:gsub("%s+", ""):gsub("%.", "") .. "Page" local Pg = Instance.new("ScrollingFrame", CA) Pg.Name = pn Pg.Size = UDim2.new(1, 0, 1, 0) Pg.BackgroundTransparency = 1 Pg.Visible = (i == 1) Pg.CanvasSize = UDim2.new(0, 0, 0, 1500) Pg.ScrollBarThickness = 3 Instance.new("UIListLayout", Pg).Padding = UDim.new(0, 6)local btn = Instance.new("TextButton", SB) btn.Size = UDim2.new(1, -10, 0, 22) btn.BackgroundColor3 = (i == 1) and Color3.fromRGB(20, 30, 20) or Color3.fromRGB(15, 17, 15) btn.Text = t btn.TextColor3 = (i == 1) and TC or Color3.fromRGB(140, 150, 140) btn.Font = Enum.Font.GothamSemibold btn.TextSize = t:find("Administracja") and 8 or 10 Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 4)if i == 1 then table.insert(E2C, btn) end table.insert(ab, { B = btn, Active = (i == 1) })btn.MouseButton1Click:Connect(function()if t == "ggFVIPWLbey" and not SprawdzDostepDoTabu() then return endfor _, p in pairs(CA:GetChildren()) do if p:IsA("ScrollingFrame") then p.Visible = false end endfor _, a in pairs(ab) do a.Active = false a.B.BackgroundColor3 = Color3.fromRGB(15, 17, 15) a.B.TextColor3 = Color3.fromRGB(140, 140, 140) for k, e in ipairs(E2C) do if e == a.B then table.remove(E2C, k) end end endPg.Visible = true btn.BackgroundColor3 = Color3.fromRGB(20, 30, 20) btn.TextColor3 = TC table.insert(E2C, btn) for _, a in pairs(ab) do if a.B == btn then a.Active = true end endend)endlocal function updateTheme(c) TC = c for _, e in pairs(E2C) do pcall(function() if e:IsA("UIStroke") then e.Color = c elseif e.Name == "SubTitle" or e:IsA("TextButton") then e.TextColor3 = c end end) end endlocal function CreateToggle(pg, txt, cb)local f = Instance.new("Frame", pg) f.Size = UDim2.new(1, -10, 0, 35) f.BackgroundColor3 = Color3.fromRGB(20, 25, 20) Instance.new("UICorner", f).CornerRadius = UDim.new(0, 6)local lbl = Instance.new("TextLabel", f) lbl.Size = UDim2.new(1, -60, 1, 0) lbl.Position = UDim2.new(0, 10, 0, 0) lbl.BackgroundTransparency = 1 lbl.Text = txt lbl.TextColor3 = Color3.fromRGB(230, 230, 230) lbl.Font = Enum.Font.Gotham lbl.TextSize = 12 lbl.TextXAlignment = Enum.TextXAlignment.Leftlocal b = Instance.new("TextButton", f) b.Size = UDim2.new(0, 40, 0, 20) b.Position = UDim2.new(1, -50, 0.5, -10) b.BackgroundColor3 = Color3.fromRGB(35, 40, 35) b.Text = "" Instance.new("UICorner", b).CornerRadius = UDim.new(0, 10)local ind = Instance.new("Frame", b) ind.Size = UDim2.new(0, 14, 0, 14) ind.Position = UDim2.new(0, 3, 0.5, -7) ind.BackgroundColor3 = Color3.fromRGB(140, 140, 140) Instance.new("UICorner", ind).CornerRadius = UDim.new(0, 7)local en = falselocal function Kliknij(wymus)if wymus ~= nil then en = wymus else en = not en endlocal tp = en and UDim2.new(1, -17, 0.5, -7) or UDim2.new(0, 3, 0.5, -7) local tc = en and TC or Color3.fromRGB(140, 140, 140) local bg = en and Color3.fromRGB(0, 50, 25) or Color3.fromRGB(35, 40, 35) TS:Create(ind, TweenInfo.new(0.2), { Position = tp, BackgroundColor3 = tc }):Play() TS:Create(b, TweenInfo.new(0.2), { BackgroundColor3 = bg }):Play() task.spawn(cb, en)endb.MouseButton1Click:Connect(function() Kliknij() end)AktywaneToggles[txt] = { Galka = Kliknij, Status = function() return en end }end-- ZAKŁADKA REKORDlocal Rp = CA.RekordPagelocal statusLbl = Instance.new("TextLabel", Rp) statusLbl.Size = UDim2.new(1, -10, 0, 35) statusLbl.BackgroundTransparency = 1 statusLbl.Text = "System zapisu funkcji. Kliknij Save aby zapamietac." statusLbl.TextColor3 = Color3.fromRGB(200, 200, 200) statusLbl.Font = Enum.Font.GothamSemibold statusLbl.TextSize = 11CreateButton(Rp, "💾 SAVE (Zapisz obecne funkcje)", function()ZapisaneFunkcje = {} local licznik = 0for nazwa, dane in pairs(AktywaneToggles) do if dane.Status() == true then ZapisaneFunkcje[nazwa] = true licznik = licznik + 1 end endstatusLbl.Text = "Zapisano pomyślnie! Zapamietane funkcje: " .. licznik statusLbl.TextColor3 = TCend)CreateButton(Rp, "▶️ PLAY (Wczytaj z opoznieniem/lagiem)", function()statusLbl.Text = "Wczytywanie... Wywolywanie laga konfiguracji..." statusLbl.TextColor3 = Color3.fromRGB(255, 165, 0)local t = os.clock() while os.clock() - t < 0.6 do endlocal odpalone = 0for nazwa, dane in pairs(AktywaneToggles) do if ZapisaneFunkcje[nazwa] then dane.Galka(true) odpalone = odpalone + 1 else dane.Galka(false) end endstatusLbl.Text = "Gotowe! Wywolano lag i automatycznie wlaczono: " .. odpalone .. " funkcji!" statusLbl.TextColor3 = TCend)-- HACKER CHAT & ADMINISTRACJAif MaDostepDoKadry(LP.Name) thenlocal Hcp = CA.HackerChatPagelocal chatDisplay = Instance.new("TextLabel", Hcp) chatDisplay.Size = UDim2.new(1, -10, 0, 120) chatDisplay.BackgroundColor3 = Color3.fromRGB(10, 15, 10) chatDisplay.Text = "[Hacker Chat]: Witamy w ukrytym kanale ekipy skycitersongg...\n" chatDisplay.TextColor3 = Color3.fromRGB(0, 255, 120) chatDisplay.TextSize = 10 chatDisplay.Font = Enum.Font.Code chatDisplay.TextYAlignment = Enum.TextYAlignment.Top chatDisplay.TextXAlignment = Enum.TextXAlignment.Left Instance.new("UICorner", chatDisplay)local chatBox = Instance.new("TextBox", Hcp) chatBox.Size = UDim2.new(1, -10, 0, 35) chatBox.BackgroundColor3 = Color3.fromRGB(15, 20, 15) chatBox.Text = "Wpisz tajna wiadomosc do ekipy..." chatBox.TextColor3 = Color3.fromRGB(200, 200, 200) chatBox.Font = Enum.Font.Gotham chatBox.TextSize = 11 Instance.new("UICorner", chatBox)chatBox.FocusLost:Connect(function(enter) if enter and chatBox.Text ~= "" then chatDisplay.Text = chatDisplay.Text .. "[" .. LP.Name .. "]: " .. chatBox.Text .. "\n" chatBox.Text = "" end end)endif LP.Name == "Demon0x0x1" thenlocal Adp = CA.AdministracjaDemon0x0x1Pagelocal blBox = Instance.new("TextBox", Adp) blBox.Size = UDim2.new(1, -10, 0, 25) blBox.BackgroundColor3 = Color3.fromRGB(25, 15, 15) blBox.Text = "Modyfikacja Blacklist serwera (Nick)..." blBox.TextColor3 = Color3.fromRGB(200, 200, 200) Instance.new("UICorner", blBox)CreateButton(Adp, "Zatwierdz Zmiane statusu Blacklist", function() if blBox.Text ~= "" then _G.CzarnaLista[blBox.Text] = not _G.CzarnaLista[blBox.Text] blBox.Text = "Zmieniono status dla: " .. blBox.Text end end)local wlBox = Instance.new("TextBox", Adp) wlBox.Size = UDim2.new(1, -10, 0, 25) wlBox.BackgroundColor3 = Color3.fromRGB(25, 15, 35) wlBox.Text = "Modyfikacja Whitelisty serwera (Nick)..." wlBox.TextColor3 = Color3.fromRGB(220, 200, 255) Instance.new("UICorner", wlBox) wlBox.FocusLost:Connect(function() if wlBox.Text ~= "" then _G.BialaLista[wlBox.Text] = true wlBox.Text = "Dodano na Whiteliste na tym serwerze!" end end)local r1 = Instance.new("TextBox", Adp) r1.Size = UDim2.new(1, -10, 0, 25) r1.BackgroundColor3 = Color3.fromRGB(20, 25, 20) r1.Text = "Modyfikacja Wspolpracownik serwera (Nick)..." r1.TextColor3 = Color3.fromRGB(200,200,200) Instance.new("UICorner", r1) r1.FocusLost:Connect(function() if r1.Text ~= "" then _G.Wspolpracownicy[r1.Text] = true r1.Text = "Dodano Wspolpracownika na tym serwerze!" end end)local r2 = Instance.new("TextBox", Adp) r2.Size = UDim2.new(1, -10, 0, 25) r2.BackgroundColor3 = Color3.fromRGB(20, 25, 20) r2.Text = "Modyfikacja Tester serwera (Nick)..." r2.TextColor3 = Color3.fromRGB(200,200,200) Instance.new("UICorner", r2) r2.FocusLost:Connect(function() if r2.Text ~= "" then _G.TesterzyRangi[r2.Text] = true r2.Text = "Dodano Testera na tym serwerze!" end end)local r3 = Instance.new("TextBox", Adp) r3.Size = UDim2.new(1, -10, 0, 25) r3.BackgroundColor3 = Color3.fromRGB(20, 25, 20) r3.Text = "Modyfikacja Moderator serwera (Nick)..." r3.TextColor3 = Color3.fromRGB(200,200,200) Instance.new("UICorner", r3) r3.FocusLost:Connect(function() if r3.Text ~= "" then _G.ModeratorzyRangi[r3.Text] = true r3.Text = "Dodano Moderatora na tym serwerze!" end end)end-- RESZTA ZAKŁADEK VOIDWARElocal Mp = CA.MainPageCreateToggle(Mp,"Infinie Saplings",function(s) end) CreateToggle(Mp,"Entity Godmode (Become Invincible)",function(s) end) CreateSlider(Mp,"Hip Height Changer",0,100,20,function(v) if LP.Character and LP.Character:FindFirstChild("Humanoid") then LP.Character.Humanoid.HipHeight=v end end) CreateToggle(Mp,"Kill Aura",function(s) _G.ka=s while _G.ka do pcall(function() local t=LP.Character:FindFirstChildOfClass("Tool") if t then for _,v in pairs(workspace:GetChildren()) do if v:FindFirstChild("Humanoid") and v.Name~=LP.Name and (v.Name:find("Cultist") or v.Name:find("Deer") or v.Name:find("Wolf") or v.Name:find("Ram")) then if (LP.Character.HumanoidRootPart.Position-v.HumanoidRootPart.Position).Magnitude<30 then t:Activate() task.wait(0.05) end end end end end) task.wait(0.1) end end) CreateToggle(Mp,"Ice Aura",function(s) end) CreateToggle(Mp,"Ore Aura",function(s) if s then game:GetService("StarterGui"):SetCore("SendNotification",{Title="Voidware",Text="Ore Aura Enabled! Make sure to equip a tool",Duration=4}) end end) CreateToggle(Mp,"Range Circle",function(s) end) CreateToggle(Mp,"Auto Chop Trees",function(s) end) CreateSlider(Mp,"Auto Chop Trees Range",0,500,200,function(v) end) CreateToggle(Mp,"Auto Plant Saplings",function(s) end) CreateToggle(Mp,"Chop Status Visualiser",function(s) end) CreateToggle(Mp,"Auto Eat",function(s) end) CreateToggle(Mp,"Auto Bandage",function(s) end) CreateToggle(Mp,"Hitbox Expansion",function(s) end) CreateToggle(Mp,"Instant Open Chests",function(s) end)local Ap = CA.AutoPageCreateToggle(Ap,"Auto Chest [BETA]",function(s) end) CreateSlider(Ap,"Plant/Build Radius",0,300,130,function(v) end) CreateSlider(Ap,"Angle Increment",0,10,2,function(v) end) CreateSlider(Ap,"Plant Height",0,50,0,function(v) end) CreateSlider(Ap,"Plant/Build Limit",0,500,250,function(v) end) CreateButton(Ap,"Plant Saplings",function() end) CreateButton(Ap,"Build Log Walls in Circle",function() end)local Dfp = CA.DayFarmPageCreateButton(Dfp,"Teleport All Trees [PATCHED]",function() end) CreateButton(Dfp,"Teleport All BIG Trees [PATCHED]",function() end) CreateButton(Dfp,"Teleport All Chests",function() end) CreateButton(Dfp,"Teleport All Children [BETA]",function() end) CreateButton(Dfp,"Teleport The Entities [PATCHED]",function() end) CreateButton(Dfp,"Freeze The Thingys",function() end) CreateButton(Dfp,"UnFreeze The Thingys",function() end)local Bp = CA.BringStuffPageCreateToggle(Bp,"Bring Cultists",function(s) end) CreateToggle(Bp,"Bring Meteor Items",function(s) end) CreateToggle(Bp,"Bring Fuel [BETA]",function(s) end) CreateToggle(Bp,"Bring Food",f
